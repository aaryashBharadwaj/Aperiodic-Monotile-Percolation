# Marks this folder as an importable package (so `from builders.module import ...` works).
# Without a clear marker, stray folders on sys.path could get treated as packages and shadow real modules.
"""Visualiser: render tilings and plot results."""

