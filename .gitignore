# Python / numba caches
__pycache__/
*.pyc
*.nbc
*.nbi

# run logs (progress spam, not results)
*.log

# machine-specific GUI timing calibration (regenerated by "Recalibrate timer"; not shared).
# data/gui_geometry.json IS tracked -- it's the machine-independent precomputed geometry.
data/gui_calib.json

# The user's own GUI/percolate runs + job checkpoints (scratch, regenerated at runtime). The
# CANONICAL paper results live in paper_results/ (TRACKED, shipped with the GUI).
results_output/
